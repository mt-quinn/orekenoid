node_modules/
dist/
test-results/
playwright-report/
.DS_Store

# Playwright capture output (regenerated by `npm run test:browser`)
webgl-*.png
coach-*.png
phone-*.png
pause-*.png
home-*.png
render-diagnostics.png

# Reference material: local clones of tModLoader, noita-telescope and
# Terraria-Generation, studied for the worldgen work. 356 MB of other people's
# repositories, and nothing in the build reads them.
reference files/

# Machine-local Claude Code permissions. launch.json is shared; this is not.
.claude/settings.local.json
